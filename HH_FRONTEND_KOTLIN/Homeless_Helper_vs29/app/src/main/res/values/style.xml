<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="MenuItemTextAppearance" parent="@android:style/TextAppearance.Widget.IconMenu.Item">
        <item name="android:textSize">16sp</item> <!-- Cambia el tamaño de la letra aquí -->
        <item name="android:textStyle">bold</item> <!-- Cambia el estilo de la letra aquí (bold, italic, normal) -->
        <item name="android:textColor">#FF0000</item> <!-- Cambia el color de la letra aquí (formato hexadecimal) -->
    </style>


    <!-- APARIENCIA PARA EL MENU DE NAVEGACION -->
    <style name="CustomTextAppearance" parent="@android:style/TextAppearance">
        <item name="android:textColor">#0000FF</item> <!-- Cambia el color de la letra a azul -->
        <item name="android:textSize">15sp</item> <!-- Cambia el tamaño de la letra a 20sp -->
        <!--  <item name="android:textStyle">bold</item>
        -->
    </style>


    <!-- APARIENCIA PARA EL BOTÓN -->
    <style name="StyleButtonHH" parent="@android:style/Widget.Button">
        <item name="android:textColor">@color/BTN_letra</item>
        <item name="android:background">@color/BTN_fondo</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textStyle">bold</item>
    </style>






</resources>